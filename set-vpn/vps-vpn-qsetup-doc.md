# 📄 vps-vpn-qsetup-doc.md – Документация

---

## 1. **Въведение**
Цел на скрипта:  
> Създава и конфигурира WireGuard VPN на VPS, управлява клиенти (рутери, сървъри, мобилни устройства) за достъп до частната мрежа NetGalaxy.

---

## 2. **Характеристики**
- Автоматична инсталация и конфигурация на WireGuard.
- Създаване на VPN мрежа (по подразбиране `10.20.0.0/24`).
- Генериране на конфигурация за клиенти.
- Поддръжка на множество клиенти.
- Без интерактивни въпроси по време на изпълнение.

---

## 3. **Изисквания**
- ОС: Ubuntu 22.04 / 24.04 (или Debian 11 / 12).
- Root достъп.
- Отворен UDP порт (по подразбиране `51820`).

---

## 4. **Как работи скриптът**
- Проверява дали WireGuard е инсталиран.
- Ако не е → инсталира пакета.
- Настройва основния VPN интерфейс (`wg0`).
- Генерира конфигурация за клиенти (peer конфигурация).
- Записва данните в:
  - `/etc/wireguard/wg0.conf` (сървърна конфигурация).
  - `/etc/netgalaxy/vpn-clients/` (конфигурации за клиенти).

---

## 5. **Структура на модулите**
- **VPN-1:** Предварителни проверки  
- **VPN-2:** Инсталиране на WireGuard  
- **VPN-3:** Създаване на основен VPN интерфейс (wg0)  
- **VPN-4:** Генериране на конфигурация за нов клиент  
- **VPN-5:** Добавяне на клиента в сървърната конфигурация  
- **VPN-6:** Рестартиране и проверка на WireGuard  
- **VPN-7:** Обобщение  

---

## 6. **Пример за клиентска конфигурация (.conf)**

[Interface]
PrivateKey = <client_private_key>
Address = 10.20.0.10/24
DNS = 10.20.0.254

[Peer]
PublicKey = <server_public_key>
Endpoint = <SERVER_PUBLIC_IP>:51820
AllowedIPs = 0.0.0.0/0
PersistentKeepalive = 25

- Рестарт на WireGuard за прилагане на промяната.

### **VPN-6: Проверка на връзката**
- Изпълнение на `wg show` за потвърждение.

### **VPN-7: Обобщение**
- Извеждане на пътя до клиентския `.conf` файл.
- Показване на QR код (ако е активирана опция за мобилен клиент).

---

### ✅ Модул 1: Предварителни проверки и инициализация  
**Файл:** `vps-vpn-qsetup.sh`  

---

#### **Какво прави модулът**
- Гарантира, че скриптът се стартира при коректни условия:
  - Проверка за **root права**.
  - Потвърждава, че е изпълнена **базовата конфигурация** (чрез `vps-base-qsetup.sh`).
  - Забранява повторно изпълнение, ако VPN вече е конфигуриран.
  - Проверява **операционната система** (поддържани: Ubuntu 22.04/24.04, Debian 11/12).
  - Изисква **потвърждение на публичния IP** (сравнява въведения с реалния чрез `curl ifconfig.me`).

---

#### **Как работи**
1. Проверява **root достъп** с `$EUID`.
2. Валидира наличието на файл `/etc/netgalaxy/setup.env` и статус `SETUP_VPS_BASE_STATUS=✅`.
3. Чете OS информация от `/etc/os-release` и сравнява с допустимите версии.
4. Изисква от потребителя да въведе публичния IP и го сравнява с реалния IP на сървъра.
5. Записва IP адреса във файла `/etc/netgalaxy/todo.modules`.
6. Отбелязва успешното изпълнение в `/etc/netgalaxy/setup.env` с `VPN_RESULT_MODULE1=✅`.

---

✔ **Резултат:**  
Сървърът преминава всички проверки и е готов за инсталация на VPN (следващите модули).

---

### ✅ Модул 2: Инсталиране на WireGuard  
**Файл:** `vps-vpn-qsetup.sh`  

---

#### **Какво прави модулът**
- Инсталира VPN софтуера **WireGuard** и необходимите инструменти за работа.
- Гарантира, че програмата е налична и функционира преди преминаване към следващите модули.

---

#### **Как работи**
1. Проверява дали модулът вече е изпълнен (`VPN_RESULT_MODULE2=✅`).
2. Обновява списъка с пакети чрез `apt-get update`.
3. Инсталира `wireguard` и `wireguard-tools` с `apt-get install`.
4. Проверява наличието на командата `wg` за потвърждение на успешна инсталация.
5. Записва резултат в `/etc/netgalaxy/setup.env` с `VPN_RESULT_MODULE2=✅`.

---

✔ **Резултат:**  
WireGuard е инсталиран и готов за конфигуриране на VPN сървъра.

---

### ✅ Модул 3: Конфигурация на основния VPN интерфейс (wg0)  
**Файл:** `vps-vpn-qsetup.sh`  

---

#### **Какво прави модулът**
- Настройва основния интерфейс **wg0** за VPN връзки.
- Събира необходимите параметри (VPN подмрежа, IP адрес, порт).
- Генерира ключове и създава конфигурационен файл за WireGuard.
- Стартира и активира услугата `wg-quick@wg0`.

---

#### **Как работи**
1. **Проверява** дали модулът е изпълнен (`VPN_RESULT_MODULE3=✅`).
2. **Събира данни от оператора**:
   - VPN подмрежа (CIDR формат, напр. `10.20.0.0/24`).
   - IP адрес на сървъра в VPN мрежата.
   - Порт за WireGuard (по подразбиране `51820`).
3. **Записва настройките** в `/etc/netgalaxy/todo.modules` за бъдеща употреба.
4. **Генерира ключове** (private/public) чрез `wg genkey` и `wg pubkey`.
5. **Създава конфигурационен файл** `/etc/wireguard/wg0.conf` със следните секции:
   - `[Interface]` с IP адрес, порт и PrivateKey.
   - `PostUp/PostDown` команди за IP forwarding.
6. **Активира wg0 интерфейса** и го стартира при boot (`systemctl enable --now wg-quick@wg0`).
7. **Записва резултат** в `/etc/netgalaxy/setup.env` (`VPN_RESULT_MODULE3=✅`).

---

✔ **Резултат:**  
VPN интерфейсът **wg0** е конфигуриран и активен. Сървърът е готов да приема клиенти (които ще се конфигурират в отделен скрипт).

---

### ✅ Модул 4: Финализиране и обобщение  
**Файл:** `vps-vpn-qsetup.sh`  

---

#### **Какво прави модулът**
- Проверява дали **WireGuard интерфейсът (wg0)** е активен.
- Извежда обобщение на основните параметри на конфигурацията (публичен IP, VPN мрежа, VPN IP на сървъра, порт, път до конфигурационния файл).
- Изисква потвърждение от оператора дали конфигурацията е приета като завършена.
- Ако бъде потвърдена:
  - Записва финален статус `SETUP_VPS_VPN_STATUS=✅` в `/etc/netgalaxy/setup.env`.
  - Премахва временния файл `todo.modules`.
  - Изтрива самия скрипт за сигурност.

---

#### **Как работи**
1. Проверява активността на услугата `wg-quick@wg0` чрез `systemctl is-active`.
2. Зарежда данните от `todo.modules`:
   - `SERVER_IP` – публичен IP на сървъра.
   - `VPN_SUBNET` – дефинираната VPN подмрежа.
   - `VPN_SERVER_IP` – IP на сървъра в VPN мрежата.
   - `WG_PORT` – използваният порт за WireGuard.
3. Извежда тези параметри на екрана за преглед.
4. Изисква от оператора потвърждение (`y/n`):
   - **При "y"** → записва финалния статус и извършва почистване.
   - **При "n"** → прекратява изпълнението без промени.
5. Извършва почистване:
   - Изтрива `todo.modules`.
   - Премахва самия скрипт.

---

✔ **Резултат:**  
След потвърждение от оператора конфигурацията е приета за завършена, а системата е маркирана със статус `SETUP_VPS_VPN_STATUS=✅`.
