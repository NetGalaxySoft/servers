# I. ПРЕДНАЗНАЧЕНИЕ

Скриптът `vps-base-qsetup.sh` представлява автоматизирано решение за базова конфигурация на VPS сървър, изпълнявано **отдалечено чрез SSH** от локален компютър. Той е част от инфраструктурата на платформата **NetGalaxy**.

## Основни цели:

- 🚀 Бързо и сигурно инициализиране на нови сървъри
- 🔒 Утвърждаване на минимални стандарти за сигурност, достъп и автоматизация
- 🧠 Подпомагане на обучението на млади системни администратори чрез модулен и прозрачен подход

## Характеристики:

- Контролирано въвеждане на параметри с потвърждение от оператора
- Изпълнение на действия **само след одобрение**
- Обобщение на резултатите в края на изпълнението
- Без прекъсване, освен при изрично прекратяване от потребителя

Скриптът осигурява **унифицирана, предвидима и стабилна начална среда** за сървъри, включени в платформата NetGalaxy или в други критични проекти.

# II. СТРУКТУРА

Скриптът `vps-base-qsetup.sh` е изграден от ясно разграничени **нумерирани модули**, всеки със собствено заглавие, цел и контрол на изпълнението. Това позволява лесно проследяване, дебъгване и повторно изпълнение при нужда.

## Формат на модулите:

Всеки модул следва шаблон:
- Заглавие с номер и описание (пример: `[5] КОНФИГУРИРАНЕ НА LOCALE`)
- Променлива `MODULE_NAME` за идентификация
- Проверка дали модулът вече е изпълнен
- Основна логика на изпълнение
- Променлива `RESULT_...` за резултата от модула
- Запис в системния файл `/etc/netgalaxy/setup.env`
- Добавяне в `/etc/netgalaxy/todo.modules`

## Основни файлове и променливи:

| Път/Име                       | Описание |
|-------------------------------|----------|
| `/etc/netgalaxy/setup.env`    | Централен конфигурационен файл с резултатите от всеки модул |
| `/etc/netgalaxy/todo.modules` | Списък на вече изпълнените модули |
| `MODULE_NAME`                 | Уникално име на модула (пример: `mod_03_system_update`) |
| `RESULT_*`                    | Резултатна стойност: `✅`, `❌`, `⚠️`, `❔` |

## Примерни модули (част от реалната структура):

- `[3] ОБНОВЯВАНЕ НА СИСТЕМАТА`
- `[4] ИНСТАЛИРАНЕ НА ОСНОВНИ ИНСТРУМЕНТИ`
- `[6] НАСТРОЙКА НА ВРЕМЕ И ЧАСОВА ЗОНА`
- `[10] ПРОМЯНА НА SSH ПОРТА`
- `[14] ОБОБЩЕНИЕ НА РЕЗУЛТАТИТЕ`

Модулният подход позволява **гъвкаво разширение** и прецизна поддръжка от специалисти на различни нива.

# III. ПРИНЦИП НА ДЕЙСТВИЕ

Скриптът `vps-base-qsetup.sh` работи по предварително дефиниран, **непрекъсваем автоматизиран сценарий**, съставен от модули. Всеки модул следва логиката на:

1. **Проверка дали е изпълняван** досега (чрез `todo.modules`)
2. **Събиране на необходима информация** (ако се изисква)
3. **Изпълнение на действия**
4. **Оценка и записване на резултат**
5. **Отбелязване като завършен**

## Ключови особености:

- ✅ **Автоматично продължаване**: При прекъсване, скриптът може да се стартира отново – вече изпълнените модули се пропускат.
- ✅ **Без интеракция по време на изпълнение**: Всички решения се взимат преди започване, като скриптът работи без допълнителни въпроси.
- ✅ **Резултати на всяка стъпка**: Всеки модул записва резултатната си стойност в променлива `RESULT_...`, която се съхранява и обобщава в края.
- ✅ **Контрол на безопасността**: Скриптът не извършва действия, ако IP адресът, хостът или условията не съвпадат с очакваните стойности (ако са зададени).

Скриптът `vps-base-qsetup.sh` работи по строго дефинирана последователност от модули, като **всеки модул**:

- изпълнява една ясно определена задача (например конфигурация на SSH, настройка на локализация, инсталация на инструменти и др.);
- проверява дали вече е бил изпълнен, като използва централен файл `todo.modules`;
- при успешно приключване отбелязва себе си като изпълнен, за да се избегне повторно изпълнение при бъдещи стартирания;
- записва резултатите от изпълнението във файл `setup.env` за централизирана отчетност;
- спазва строго принципа: **"без действие без потвърждение"**, като първо събира информация и изчаква операторско потвърждение преди реални промени (когато е приложимо).

Всяка операция е придружена от визуален маркер (икони: ✅, ❌, ⚠️), който позволява бърза визуална проверка. Скриптът не разчита на външни зависимости по време на изпълнение и **работи напълно офлайн** след първоначалното изтегляне.


## Обобщение:

> Скриптът изпълнява **пълна, автоматизирана и възстановима** начална конфигурация на сървър с **максимална предвидимост и проследимост**.

# IV. ДОПЪЛНИТЕЛНА ИНФОРМАЦИЯ

- **Разработчик**: Ilko Yordanov (NetGalaxySoft)
- **Проект**: NetGalaxy Platform — автоматизирана система за поддръжка на VPS сървъри
- **Лиценз**: Частен скрипт – достъпен само чрез GitHub токен, разрешен за използване в рамките на проекта NetGalaxy
- **Версия на скрипта**: `v1.0` (19 юни 2025)
- **Местоположение в GitHub**:  
  [`https://github.com/NetGalaxySoft/servers/tree/main/set-base`](https://github.com/NetGalaxySoft/servers/tree/main/set-base)

---

Скриптът е създаден с цел обучение и улесняване на млади системни администратори без опит. Всички конфигурации са безопасни, проследими и напълно автоматизирани. Не изисква ръчна намеса, освен при предварителна проверка и потвърждение.

## 📌 Автори и поддръжка

- **Автор:** Ilko Yordanov (NetGalaxySoft)
- **Организация:** NetGalaxy Project
- **Поддръжка:** Скриптът се поддържа активно в репозитория:  
  [`https://github.com/NetGalaxySoft/servers`](https://github.com/NetGalaxySoft/servers)

## ⚠️ Ограничен лиценз

> 🔐 **Вътрешна употреба**:  
> Този скрипт е предназначен **само за използване в рамките на проекта NetGalaxy**.  
> Достъпът до кода е възможен **само чрез GitHub токен**, предоставен на оторизирани участници.  
> Разпространението и използването от външни лица е **забранено**.

## 📚 Свързани документи

- `vps-base-qsetup.sh` — главният скрипт за базова конфигурация
- `vps-host-qsetup.sh` — скрипт за хостинг-сървъри
- `vps-virhost-create.sh` — създаване на виртуални хостове (Apache)
- `netgalaxy-dev-env.sh` — подготовка на среда за разработка

## 📅 Версия на скрипта

- **Версия:** 1.0
- **Дата на завършване:** 2025-07-10
- **Последен тест:** ✔️ Успешно преминат функционален тест на сървър `vmi2406856` (VPS @ Contabo)

---

# 📌 vps-base-qsetup-doc.md

## [МОДУЛ 1] ПРЕДВАРИТЕЛНИ ПРОВЕРКИ И ИНИЦИАЛИЗАЦИЯ

### ✅ Цел
Този модул проверява средата преди начална конфигурация на VPS сървъра и инициализира структурата на NetGalaxy за по-нататъшни модули.  
Основните му задачи са:
- Проверка на операционната система и версията ѝ.
- Потвърждение на реалния публичен IP адрес.
- Създаване на базова структура от конфигурационни файлове.

---

### 🔍 Дейности в модула

1. **Проверка дали скриптът вече е изпълнен успешно**  
   - Ако файлът `/etc/netgalaxy/setup.env` съдържа `SETUP_VPS_BASE_STATUS=✅`, изпълнението спира, за да се избегне повреда на системата.

2. **Проверка на операционната система**  
   - Скриптът открива OS чрез файла `/etc/os-release`.
   - Поддържани системи:  
     - **Ubuntu**: 22.04, 24.04  
     - **Debian**: 11, 12  
   - Ако OS не е поддържана → прекратяване с грешка.

3. **Проверка дали модулът вече е изпълнен**  
   - Ако в `setup.env` има `BASE_RESULT_MODULE1=✅` → модулът се пропуска.

4. **Потвърждение на публичния IP адрес**  
   - Скриптът иска въвеждане на публичния IP адрес на сървъра.
   - Сравнява въведения IP с реалния IP, открит чрез `curl -s -4 ifconfig.me`.
   - При несъответствие:
     - Показва предупреждение.
     - Дава избор за повторен опит или прекратяване.
   - При съвпадение: потвърждава IP адреса.

5. **Създаване на структурата на NetGalaxy**  
   - Директория: `/etc/netgalaxy`
   - Файлове:
     - `todo.modules` (списък с междинни данни за конфигурация).
     - `setup.env` (файл с резултати от изпълнението на модулите).
   - Задава правилните права:
     - `setup.env` → `chmod 600`
     - `todo.modules` → `chmod 644`

6. **Запис на данни**  
   - Записва потвърдения IP адрес в `todo.modules`.
   - Записва резултата `BASE_RESULT_MODULE1=✅` в `setup.env`.

---

### ✅ Успешен завършек
- `setup.env` съдържа:

---

## [МОДУЛ 2] КОНФИГУРАЦИЯ НА СЪРВЪРНИЯ ДОМЕЙН (FQDN)

### ✅ Цел
Модулът конфигурира напълно домейна на сървъра (FQDN), задава hostname, актуализира файла `/etc/hosts` и записва информацията в конфигурационните файлове на NetGalaxy.

---

### 🔍 Дейности в модула

1. **Проверка дали модулът вече е изпълнен**
   - Ако `setup.env` съдържа `BASE_RESULT_MODULE2=✅`, модулът се пропуска.

2. **Въвеждане и валидиране на FQDN**
   - Скриптът изисква от потребителя да въведе домейн в правилен формат.
   - Проверява:
     - Непразно поле.
     - Коректен формат (пример: `example.com`).
   - Ако домейнът не резолвира, показва предупреждение и пита дали да продължи.

3. **Конфигуриране на системното име (hostname)**
   - Използва командата:
     ```
     sudo hostnamectl set-hostname <FQDN>
     ```
   - Потвърждава, че hostname е зададен.

4. **Определяне на основни променливи**
   - `SERVER_IP` → текущият публичен IP чрез `curl -s -4 ifconfig.me`.
   - `SHORT_HOST` → първата част от домейна.
   - `WWW_ALIAS` → добавяне на `www.` към FQDN.

5. **Актуализация на /etc/hosts**
   - Уверява се, че редът за `127.0.0.1 localhost` съществува.
   - Премахва стари записи за `127.0.1.1` и публичния IP.
   - Добавя:
     ```
     127.0.1.1   <FQDN> <SHORT_HOST>
     <SERVER_IP>   <FQDN> <WWW_ALIAS>
     ```
   - Проверява коректността на добавените редове.

6. **Запис в NetGalaxy конфигурационните файлове**
   - Записва `FQDN` в `/etc/netgalaxy/todo.modules`.
   - Записва резултата:
     ```
     BASE_RESULT_MODULE2=✅
     ```
     в `/etc/netgalaxy/setup.env`.

---

### ✅ Успешен завършек
- `hostnamectl status` показва коректно FQDN.
- BASE_RESULT_MODULE2=✅

---

## [МОДУЛ 3] ОБНОВЯВАНЕ НА СИСТЕМАТА

### ✅ Цел
Модулът извършва пълно обновяване на системата, включително:
- Обновяване на списъка с пакети.
- Инсталиране на всички налични актуализации.
- Премахване на ненужни пакети.

---

### 🔍 Дейности в модула

1. **Проверка дали модулът вече е изпълнен**
   - Ако в `setup.env` има `BASE_RESULT_MODULE3=✅`, модулът се пропуска.

2. **Проверка за заетост на пакетната система**
   - Скриптът следи за заключен процес (`/var/lib/dpkg/lock-frontend`).
   - Ако е заета повече от **60 секунди**, изпълнението спира с грешка.

3. **Обновяване на системата**
   - Изпълнява се последователно:
     ```bash
     sudo apt-get update
     sudo apt-get upgrade -y
     sudo apt-get autoremove -y
     ```
   - При успех: записва резултат в `setup.env`.

4. **Запис на резултат**
   - В `setup.env` се записва:
     ```
     BASE_RESULT_MODULE3=✅
     ```

---

### ✅ Успешен завършек
- Всички пакети са обновени.
- `setup.env` съдържа:
- BASE_RESULT_MODULE3=✅

---

## [МОДУЛ 4] ИНСТАЛИРАНЕ НА ОСНОВНИ ИНСТРУМЕНТИ

### ✅ Цел
Модулът проверява наличието на основни системни инструменти и ги инсталира, ако липсват. Тези инструменти са необходими за базова поддръжка и конфигурация на VPS сървъра.

---

### 🔍 Дейности в модула

1. **Проверка дали модулът вече е изпълнен**
   - Ако в `setup.env` има `BASE_RESULT_MODULE4=✅`, модулът се пропуска.

2. **Списък на задължителните пакети**
   - `nano` – текстов редактор.
   - `unzip` – разархивиране на .zip файлове.
   - `git` – управление на Git репозитории.
   - `curl` – изтегляне на файлове по HTTP/HTTPS.
   - `wget` – алтернативен инструмент за изтегляне.
   - `net-tools` – команди за мрежова диагностика (`ifconfig` и др.).
   - `htop` – интерактивен монитор на процесите.
   - `dnsutils` – инструменти за DNS диагностика (например `dig`).

3. **Проверка и инсталация на пакетите**
   - За всеки пакет:
     - Ако е инсталиран → показва „✔ <име> е вече инсталиран.“
     - Ако липсва:
       - Изпълнява:
         ```bash
         sudo apt-get install -y <пакет>
         ```
       - При успех → показва „✅ Успешно инсталиране на <пакет>“.
       - При грешка:
         - Предлага избор:
           - `[1]` Повторен опит.
           - `[2]` Продължаване с предупреждение (маркира резултат с ⚠️).
           - `[q]` Прекратяване на скрипта.

4. **Запис на резултата**
   - Ако всички пакети са инсталирани успешно → `BASE_RESULT_MODULE4=✅`.
   - Ако има пропуснати пакети → `BASE_RESULT_MODULE4=⚠️`.

---

### ✅ Успешен завършек
- Всички изброени пакети са налични.
- В `setup.env` е записано:
- BASE_RESULT_MODULE4=✅

---

## [МОДУЛ 5] НАСТРОЙКА НА ЛОКАЛИЗАЦИИ

### ✅ Цел
Модулът активира и конфигурира локализации за UTF-8, необходими за съвместимост с международната мрежа NetGalaxy. Основните езици по подразбиране са:
- **Български** (bg_BG.UTF-8)
- **Руски** (ru_RU.UTF-8)
- **Английски** (en_US.UTF-8)

---

### 🔍 Дейности в модула

1. **Проверка дали модулът вече е изпълнен**
   - Ако в `setup.env` има `BASE_RESULT_MODULE5=✅`, модулът се пропуска.

2. **Инсталиране на езикови пакети**
   - Опит за инсталиране:
     ```bash
     sudo apt-get install -y language-pack-bg language-pack-ru
     ```
   - Ако инсталацията не успее → показва предупреждение и продължава.

3. **Активиране на локализации в `/etc/locale.gen`**
   - Премахва коментари за:
     ```
     bg_BG.UTF-8 UTF-8
     ru_RU.UTF-8 UTF-8
     en_US.UTF-8 UTF-8
     ```
   - Ако липсват, добавя ги.

4. **Генериране на локализации**
   - Изпълнява:
     ```bash
     sudo locale-gen && sudo update-locale
     ```
   - При успех → записва резултатите.

5. **Запис в NetGalaxy конфигурационни файлове**
   - В `todo.modules` добавя:
     ```
     LOCALES="bg_BG.UTF-8 ru_RU.UTF-8 en_US.UTF-8"
     ```
   - В `setup.env`:
     ```
     BASE_RESULT_MODULE5=✅
     ```

---

### ✅ Успешен завършек
- BASE_RESULT_MODULE5=✅

---

## [МОДУЛ 6] НАСТРОЙКА НА ВРЕМЕВА ЗОНА И NTP СИНХРОНИЗАЦИЯ

### ✅ Цел
Модулът настройва правилна времева зона и осигурява точна синхронизация на часовника чрез NTP. Това е критично за стабилността на сървърните услуги, особено при криптографски операции и системни логове.

---

### 🔍 Дейности в модула

1. **Проверка дали модулът вече е изпълнен**
   - Ако в `setup.env` има:
     ```
     BASE_RESULT_MODULE6=✅
     ```
     модулът се пропуска.

2. **Задаване на времева зона**
   - Използва се командата:
     ```bash
     sudo timedatectl set-timezone UTC
     ```
   - Стандартът за NetGalaxy е **UTC**.

3. **Деактивиране на конфликтни NTP услуги**
   - Спира `ntpd` и `systemd-timesyncd`:
     ```bash
     sudo systemctl stop ntpd && sudo systemctl disable ntpd
     sudo systemctl stop systemd-timesyncd && sudo systemctl disable systemd-timesyncd
     ```

4. **Инсталиране и настройка на Chrony**
   - Инсталация:
     ```bash
     sudo apt-get install -y chrony
     ```
   - Конфигурация на `/etc/chrony/chrony.conf`:
     ```
     server 0.pool.ntp.org iburst
     server 1.pool.ntp.org iburst
     server 2.pool.ntp.org iburst
     server 3.pool.ntp.org iburst

     rtcsync
     makestep 1.0 3
     driftfile /var/lib/chrony/drift
     logdir /var/log/chrony
     ```

5. **Рестартиране и активиране на Chrony**
   ```bash
   sudo systemctl restart chrony
   sudo systemctl enable chrony

---

### ✅ Успешен завършек
- BASE_RESULT_MODULE6=✅

---
## [МОДУЛ 7] СЪЗДАВАНЕ НА НОВ АДМИНИСТРАТОРСКИ ПРОФИЛ

### ✅ Цел
Модулът създава нов административен потребител с root права и копира SSH ключовете от root профила за сигурен достъп. След това забранява root достъпа чрез SSH.

---

### 🔍 Дейности в модула

1. **Проверка дали модулът вече е изпълнен**
   - Ако `setup.env` съдържа:
     ```
     BASE_RESULT_MODULE7=✅
     ```
     модулът се пропуска.

2. **Информиране за сигурност**
   - Скриптът уведомява, че:
     - Ще бъде създаден администратор с root права.

3. **Въвеждане и валидиране на потребителско име**
   - Изисквания:
     - Минимум 5 символа.
     - Малки букви, цифри, тирета и долни черти.
   - Ако потребителят вече съществува:
     - Предлага да бъде използван и добавен в групата `sudo`.

4. **Въвеждане и проверка на парола**
   - Минимум 8 символа.
   - Поне една малка и една главна латинска буква.
   - Поне една цифра.
   - Две въвеждания за потвърждение.

5. **Създаване на нов потребител**
   - Ако не съществува:
     ```bash
     sudo useradd -m -s /bin/bash <username>
     echo "<username>:<password>" | sudo chpasswd
     sudo usermod -aG sudo <username>
     ```
   - Копиране на SSH ключовете от root:
     - В директория `/home/<username>/.ssh`.
     - Настройка на права (`chmod 700` за `.ssh` и `chmod 600` за файловете).

6. **Запис на информация**
   - В `todo.modules`:
     ```
     ADMIN_USER="<username>"
     ```
   - В `setup.env`:
     ```
     BASE_RESULT_MODULE7=✅
     ```

---

### ✅ Успешен завършек
- UFW е инсталиран и конфигуриран с правило за текущия SSH порт.
- Защитната стена **все още не е активирана** – това ще стане в последния модул.
- В `setup.env`:
BASE_RESULT_MODULE8=✅

---

---

## [МОДУЛ 8] КОНФИГУРИРАНЕ НА UFW И ДЕАКТИВАЦИЯ НА ДРУГИ FIREWALL ПОРТОВЕ

### ✅ Цел
Модулът инсталира и конфигурира UFW (Uncomplicated Firewall) като основна защитна стена на сървъра, премахвайки други решения като `firewalld` и изчиствайки стари iptables правила. След приключване UFW остава **деактивиран**, за да бъде активиран по-късно в контролирана стъпка.

---

### 🔍 Дейности в модула

1. **Проверка дали модулът вече е изпълнен**
   - Ако в `setup.env` има:
     ```
     BASE_RESULT_MODULE8=✅
     ```
     → модулът се пропуска.

2. **Деактивиране на други firewall системи**
   - Ако е открит `firewalld`:
     ```bash
     sudo systemctl stop firewalld
     sudo systemctl disable firewalld
     sudo apt-get remove -y firewalld
     ```

3. **Изчистване на стари iptables правила**
   - Ако има активни правила:
     ```bash
     sudo iptables -F
     ```

4. **Инсталиране на UFW**
   - Ако липсва:
     ```bash
     sudo apt-get update && sudo apt-get install -y ufw
     ```

5. **Деактивиране на UFW (ако е активен)**
   ```bash
   sudo ufw disable

6. **Засичане на текущия SSH порт**
Скриптът извлича активния SSH порт чрез:
```bash
sudo ss -tlpn | grep sshd

---

## [МОДУЛ 9] ДОБАВЯНЕ НА TRUSTED МРЕЖИ

### ✅ Цел
Модулът позволява добавяне на доверени (private/trusted) мрежи в UFW, за да се осигури достъп до сървъра от конкретни CIDR диапазони. Това е полезно за VPN тунели, вътрешни системи или локални подсистеми.

---

### 🔍 Дейности в модула

1. **Проверка дали модулът вече е изпълнен**
   - Ако в `setup.env` има:
     ```
     BASE_RESULT_MODULE9=✅
     ```
     → модулът се пропуска.

2. **Проверка за наличието на UFW**
   - Ако UFW не е инсталиран → прекратяване на скрипта.

3. **Интерактивно въвеждане на доверени мрежи**
   - Пита потребителя:
     ```
     Ще използвате ли достъп от частна (trusted) мрежа? (y / n / q)
     ```
     - `y` → въвеждане на CIDR мрежи (пример: `10.8.0.0/24`).
     - `n` → пропускане (не се добавят мрежи).
     - `q` → изход от скрипта.
   - Въвеждането приключва при празен ред.

4. **Добавяне на правилата в UFW**
   - За всяка валидна мрежа се изпълнява:
     ```bash
     sudo ufw allow from <CIDR>
     ```
   - Потвърждава с:
     ```
     ✅ Разрешен достъп от доверена мрежа: <CIDR>
     ```

5. **Запис на информация**
   - В `todo.modules`:
     ```
     TRUSTED_NETS="No"   # ако няма мрежи
     TRUSTED_NETS="10.8.0.0/24 192.168.1.0/24"  # ако има мрежи
     ```
   - В `setup.env`:
     ```
     BASE_RESULT_MODULE9=✅
     ```

---

### ✅ Успешен завършек
- Всички зададени CIDR мрежи са добавени в правилата на UFW.
- В `setup.env`:
BASE_RESULT_MODULE9=✅

---

## [МОДУЛ 10] ПРОМЯНА НА SSH ПОРТА

### ✅ Цел
Модулът позволява промяна на SSH порта, като записва настройките в `sshd_config` и добавя правилото в UFW (без активиране на защитната стена и без незабавен рестарт на SSH услугата). Това гарантира, че промяната ще бъде финализирана безопасно на по-късен етап.

---

### 🔍 Дейности в модула

1. **Проверка дали модулът вече е изпълнен**
   Ако в `setup.env` има:
   
       BASE_RESULT_MODULE10=✅
   
   → модулът се пропуска.

2. **Засичане на текущия SSH порт**
   
       sudo ss -tlpn | grep sshd

3. **Извличане на SSH порт от `todo.modules`**
   Ако вече има запис:
   
       SSH_PORT="<стойност>"
   
   В противен случай се използва текущият порт.

4. **Интерактивна промяна на порта**
   - Позволява въвеждане на нов порт (1024–65535) или запазване на текущия.
   - Въвеждането на `q` прекратява скрипта.

5. **Редакция на `sshd_config`**
   Ако портът е променен:
   
       sudo sed -i "s/^#*Port .*/Port <нов порт>/" /etc/ssh/sshd_config
   
   Промяната влиза в сила след финалния модул.

6. **Добавяне на правило в UFW (без активиране)**
   
       sudo ufw allow <SSH_PORT>/tcp comment 'Allow SSH port'

7. **Запис на информация**
   В `todo.modules`:
   
       SSH_PORT="<SSH_PORT>"
   
   В `setup.env`:
   
       BASE_RESULT_MODULE10=✅

---

### ✅ Успешен завършек
- SSH портът е определен и записан.
- `sshd_config` е актуализиран, но рестартът ще се извърши по-късно.
- В `setup.env`:
  
       BASE_RESULT_MODULE10=✅
  
- В `todo.modules`:
  
       SSH_PORT="<SSH_PORT>"

---

## [МОДУЛ 11] ОБОБЩЕНИЕ НА КОНФИГУРАЦИЯТА

### ✅ Цел
Да предостави на администратора ясен преглед на извършените настройки и статуса на всички модули, преди финалното активиране на защитната стена и рестартиране на системата.

---

### 🔍 Дейности в модула

1. **Проверка за изпълнение**
   - Ако в `setup.env` има:
     
         BASE_RESULT_MODULE11=✅
     
     → модулът се пропуска.

2. **Зареждане на временни данни от `todo.modules`**
   - Основни параметри:
     
         SERVER_IP
         FQDN
         SSH_PORT
         ADMIN_USER
         TRUSTED_NETS

3. **Четене на статуси на модулите от `setup.env`**
   - Проверяват се:
     
         BASE_RESULT_MODULE1
         BASE_RESULT_MODULE2
         ...
         BASE_RESULT_MODULE10

4. **Извеждане на отчет**
   - Показва се таблица със статуса:
     
         [1] IP адрес:            <SERVER_IP>        (статус)
         [2] FQDN:                <FQDN>             (статус)
         [7] Админ потребител:    <ADMIN_USER>       (статус)
         [10] SSH порт:           <SSH_PORT>         (статус)
         ...
         [9] Trusted мрежи:       <TRUSTED_NETS>     (статус)

---

### ✅ Примерен изход

    📋 СЪСТОЯНИЕ НА КОНФИГУРАЦИЯТА (ПО МОДУЛИ):
    [1] IP адрес на сървъра:          203.0.113.10   (✅)
    [2] FQDN (hostname):              ns1.example.com (✅)
    [3] Обновяване на системата:      ✅
    [4] Основни инструменти:          ✅
    [5] Локализации:                  ✅
    [6] Времева зона и NTP:           ✅
    [7] Администраторски профил:      adminuser      (✅)
    [8] Настройка на UFW:             ✅
    [9] Trusted мрежи:                10.8.0.0/24    (✅)
    [10] SSH порт:                    58739          (✅)

---

## [МОДУЛ 12] ФИНАЛЕН ДИАЛОГ С ОПЕРАТОРА И РЕСТАРТ

### ✅ Цел
Да позволи на администратора да потвърди дали конфигурацията е приета за успешна, да активира UFW и да рестартира системата.

---

### 🔍 Дейности в модула

1. **Финален въпрос**
   - Въпрос към оператора:
     
         Приемате ли конфигурацията като успешна?
         [y] Активиране на UFW и рестарт
         [n] Изход без промени

2. **Ако отговорът е `y`**
   - Активиране на UFW:
     
         sudo ufw --force enable
   - Запис на резултата:
     
         BASE_RESULT_MODULE11=✅
         SETUP_VPS_BASE_STATUS=✅
   - Изтриване на временни файлове:
     
         rm /etc/netgalaxy/todo.modules
   - Рестарт на системата:
     
         sudo reboot

3. **Ако отговорът е `n`**
   - Скриптът приключва без активиране на UFW и без рестарт.

---

### ✅ Успешен завършек
- UFW е активиран.
- Всички резултати са записани в `setup.env`:
  
      BASE_RESULT_MODULE11=✅
      SETUP_VPS_BASE_STATUS=✅
- Временните файлове са изтрити.
- Системата е рестартирана.


---
**NetGalaxy** – Технология с човешко лице.

